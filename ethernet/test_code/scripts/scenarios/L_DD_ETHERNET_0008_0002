# Begin L_DD_ETHERNET_0008_0002

01  set -x; handlerIfconfig.sh "get" "ipaddr"
02  set -x; handlerNfs.sh "verify"
03  set -x; handlerTmpfs.sh "create" "100" "/mnt/tmpfs/"
04  set -x; handlerNuttcp.sh "server" "start"

# Stress Process Priority N Commands No Delay

05  set -x; handlerProcessParallelism.sh "clean"

06  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
07  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
08  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
09  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
10  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
11  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
12  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
13  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
14  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"

15  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
16  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
17  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
18  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
19  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
20  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
21  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
22  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
23  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"

24  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.01 bs=1M count=5"
25  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.02 bs=1M count=5"
26  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.03 bs=1M count=5"
27  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.04 bs=1M count=5"
28  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.05 bs=1M count=5"
29  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.06 bs=1M count=5"
30  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.07 bs=1M count=5"
31  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.08 bs=1M count=5"
32  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.09 bs=1M count=5"

33  set -x; handlerNuttcp.sh "local"  "throughput" "-R50m" "9"

34  set -x; handlerProcessParallelism.sh "execute"

# Process Priority N Commands Random Delay

35  set -x; handlerProcessParallelism.sh "clean"

36  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
37  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
38  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
39  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
40  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
41  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
42  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
43  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"
44  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=/mnt/tmpfs/tmpfs.01 bs=1M count=5"

45  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
46  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
47  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
48  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
49  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
50  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
51  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
52  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
53  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"

54  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.01 bs=1M count=5"
55  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.02 bs=1M count=5"
56  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.03 bs=1M count=5"
57  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.04 bs=1M count=5"
58  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.05 bs=1M count=5"
59  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.06 bs=1M count=5"
60  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.07 bs=1M count=5"
61  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.08 bs=1M count=5"
62  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.09 bs=1M count=5"

63  set -x; handlerNuttcp.sh "local"  "throughput" "-R50m" "9"

64  set -x; handlerProcessParallelism.sh "execute" "withrandomdelay"

65  set -x; handlerNuttcp.sh "server" "stop"
66  set -x; handlerTmpfs.sh "remove" "/mnt/tmpfs/"

# End L_DD_ETHERNET_0008_0002
