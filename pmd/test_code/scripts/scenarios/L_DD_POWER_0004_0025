##############################################################################7
# Scenario: L_DD_POWER_0004_0025
# Author  : Leed Aguilar
# Date    : 11/11/10
# Testing : AV playback: Android Suspend/Resume pressing Power key during usecase
#           Boot the platform, release wakelocks, press power key to suspend,
#           then resume to ensure that system works properly. Repeat this process
#           200 times
# Results : At resume, you should get the console prompt back and there should
#           be any lockup and OSWR and DEVICE-OFF counters should increase
###############################################################################

# Begin L_DD_POWER_0004_0025

0001 set -x; is_android_ready.sh

# Set the max screen timeout (30 min)
0002 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "1800000"

# set a dummy wakelock to avoid going to suspend
0003 set -x; handlerAndroidPM.sh wakelock hold dummy

# Verify that the screen is ON
0004 set -x; android_display_switch.sh ON

# Iterate 200 times the following process:
# a) start AV playback usecase
# b) stop AV playback usecase
# c) suspend the system through suspend path
# d) resume and verify that system works fine

0005 set -x; handlerCmd.sh iterate 'handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "before"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "before"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "before"; handlerActivityManager.sh "av_playback_ics" start $MP4_FILE; sleep 10; handlerAndroidPM.sh suspend android force 5; handlerAndroidPM.sh resume android; handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "after"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "after"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET" "before" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET-LOGIC-OFF" "before" "after"; handlerPowerTransitionStats.sh compare "DEVICE-OFF" "DEVICE-OFF" "before" "after"' 200 2

# End L_DD_POWER_0004_0025
