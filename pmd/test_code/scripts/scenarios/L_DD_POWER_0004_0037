##############################################################################7
# Scenario: L_DD_POWER_0004_0037
# Author  : Leed Aguilar
# Date    : 16/20/11
# Testing : Execute AV Record usecase and perform suspend/resume (press F2) in
#           the middle of the record operation: Boot the platform, release
#           wakelocks, press power key to suspend and then resume to ensure that
#           system works properly. Repeat this process 200 times
# Results : At resume, you should get the console prompt back and there should
#           be any lockup and OSWR and DEVICE-OFF counters should increase
###############################################################################

# Begin L_DD_POWER_0004_0037

0000 set -x; is_android_ready.sh

# Set the max screen timeout (30 min)
0001 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "1800000"

# set a dummy wakelock to avoid going to suspend
0002 set -x; handlerAndroidPM.sh wakelock hold dummy

# Verify that the screen is ON
0003 set -x; android_display_switch.sh ON

0004 set -x; handlerActivityManager.sh  av_record start && sleep 3

# Iterate 200 times the following process:
# a) start/stop AV record usecase
# b) suspend the system through suspend path
# c) resume and verify that system works fine
# d) check for OSWR and Device OFF counters

0005 set -x; handlerCmd.sh iterate 'handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "before"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "before"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "before"; handlerAndroidMonkey.sh keypad 1 1000 KeyMonkeyDpadRight; handlerAndroidMonkey.sh keypad 1 1000 KeyMonkeyDpadCenter; sleep 15; handlerAndroidPM.sh suspend android force 5; handlerAndroidPM.sh resume android; handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "after"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "after"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET" "before" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET-LOGIC-OFF" "before" "after"; handlerPowerTransitionStats.sh compare "DEVICE-OFF" "DEVICE-OFF" "before" "after"; rm /sdcard/DCIM/Camera/VID*.mp4' 200 2

0006 set -x; handlerAndroidMonkey.sh keypad 1 1000 KeyMonkeyHome
0007 set -x; handlerActivityManager.sh  av_record stop

# End L_DD_POWER_0004_0037
