###############################################################################
# Scenario: L_DD_POWER_0004_0023
# Author  : Leed Aguilar
# Date    : 11/11/10
# Testing : Run and complete AV playback usecase then perform suspend/resume
#           via timeout: Boot platform, release wakelocks, let the system go
#           to suspend via timeout, then resume to ensure that system works
#           properly. Repeat this process 200 times
# Results : At resume, you should get the console prompt back and there should
#           be any lockup and OSWR and DEVICE-OFF counters should increase
###############################################################################

# Begin L_DD_POWER_0004_0023

0000 set -x; is_android_ready.sh

# Set the max screen timeout (30 min)
0001 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "15000"

# set a dummy wakelock to avoid going to suspend
0002 set -x; handlerAndroidPM.sh wakelock hold dummy

# Verify that the screen is ON
0003 set -x; android_display_switch.sh ON

# Iterate 200 times the following process:
# a) set screen timeout to the max value
# b) start AV playback usecase
# c) stop AV playback usecase
# d) set screen timeout the minimun value
# e) suspend the system via timeout
# f) resume and verify that system works fine

0004 set -x; handlerCmd.sh iterate 'handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "before"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "before"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "before"; handlerActivityManager.sh "av_playback_ics" start $MP4_FILE; sleep 15; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyHome; sleep 2; handlerActivityManager.sh "av_playback_ics" stop; handlerAndroidPM.sh suspend android timeout 5; handlerAndroidPM.sh resume android; handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "after"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "after"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET" "before" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET-LOGIC-OFF" "before" "after"; handlerPowerTransitionStats.sh compare "DEVICE-OFF" "DEVICE-OFF" "before" "after"; android_display_switch.sh ON' 200 2

0005 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "1800000"

# End L_DD_POWER_0004_0023
