###############################################################################
# Scenario: L_DD_POWER_0004_0029
# Author  : Leed Aguilar
# Date    : 11/11/10
# Testing : Run and complete Audio record usecase then perform suspend/resume
#           via timeout: Boot platform, release wakelocks, let the system go to
#           suspend via timeout, then resume to ensure that system works
#           properly. Repeat this process 200 times
# Results : At resume, you should get the console prompt back and there should
#           be any lockup and OSWR and DEVICE-OFF counters should increase
###############################################################################

# Begin L_DD_POWER_0004_0029

0000 set -x; is_android_ready.sh

# Set the max screen timeout (30 min)
0001 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "15000"

# set a dummy wakelock to avoid going to suspend
0002 set -x; handlerAndroidPM.sh wakelock hold dummy

# Iterate the following process 200 times
# a) start audio record usecase
# b) stop audio record usecase
# c) suspend the system in suspend path
# d) resume the system

0003 set -x; handlerCmd.sh iterate 'handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "before"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "before"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "before"; android_display_switch.sh ON; handlerActivityManager.sh audio_record start; sleep 1; handlerActivityManager.sh audio_record start && sleep 1; handlerAndroidMonkey.sh keypad 3 800 KeyMonkeyDpadLeft; handlerAndroidMonkey.sh keypad 3 800 KeyMonkeyDpadDown; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyDpadCenter; sleep 10; handlerAndroidMonkey.sh keypad 3 800 KeyMonkeyDpadDown; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyDpadCenter; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyHome; handlerActivityManager.sh audio_record stop ; sleep 2;  handlerAndroidPM.sh suspend android timeout 5; handlerAndroidPM.sh resume android; handlerPowerTransitionStats.sh log "core_pwrdm" "RET" "after"; handlerPowerTransitionStats.sh log "core_pwrdm" "RET-LOGIC-OFF" "after"; handlerPowerTransitionStats.sh log "DEVICE-OFF" "DEVICE-OFF" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET" "before" "after"; handlerPowerTransitionStats.sh compare "core_pwrdm" "RET-LOGIC-OFF" "before" "after"; handlerPowerTransitionStats.sh compare "DEVICE-OFF" "DEVICE-OFF" "before" "after"; rm /sdcard/recording*.3gpp' 200 2

0004 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "1800000"

# End L_DD_POWER_0004_0029
