##############################################################################
# Scenario: L_DD_AUDIO-ALSA_0102
# Author  : Misael Lopez Cruz
# Date    : 07 June 2011
##############################################################################

# Begin L_DD_AUDIO-ALSA_0102T

0001	set -x; $TESTSCRIPT/ConfMix.sh 'Analog Left Capture Route' 'Main Mic' && exit 0 || exit 1
0002	set -x; $TESTSCRIPT/ConfMix.sh 'Analog Right Capture Route' 'Sub Mic' && exit 0 || exit 1
0003	set -x; $TESTSCRIPT/ConfMix.sh 'Analog Left Capture Route' 'Headset Mic' && $TESTSCRIPT/ConfMix.sh 'Analog Right Capture Route' 'Headset Mic' && exit 0 || exit 1
0004	set -x; $TESTSCRIPT/ConfMix.sh  "DMIC1 UL Volume" "140" && exit 0 || exit 1
0005	set -x; $TESTSCRIPT/ConfMix.sh 'Analog Left Capture Route' 'Aux/FM Left' &&  $TESTSCRIPT/ConfMix.sh 'Analog Right Capture Route' 'Aux/FM Right' && exit 0 || exit 1
0006	set -x; $TESTSCRIPT/ConfMix.sh 'Earphone Enable Switch' 1 && $TESTSCRIPT/ConfMix.sh 'Earphone Playback Volume' 12 && exit 0 || exit 1
0007	set -x; $TESTSCRIPT/ConfMix.sh 'HF Left Playback' 'HF DAC' && $TESTSCRIPT/ConfMix.sh 'HF Right Playback' 'HF DAC' && exit 0 || exit 1
0008	set -x; $TESTSCRIPT/ConfMix.sh 'HS Left Playback' 'HS DAC' && $TESTSCRIPT/ConfMix.sh 'HS Right Playback' 'HS DAC' && exit 0 || exit 1

# End L_DD_AUDIO-ALSA_0102T
