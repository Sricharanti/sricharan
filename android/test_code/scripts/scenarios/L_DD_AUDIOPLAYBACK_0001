###############################################################################
# Scenario: L_DD_AUDIOPLAYBACK_0001
# Author  : Leed Aguilar
# Date    : 11/11/10
# Testing : Run and complete Audio playback usecase then perform suspend/resume
#           in suspend path (pressing Power/F2 key): Boot the platform, release
#           wakelocks, press power key to suspend and then resume to ensure that
#           system works properly
# Results : At resume, you should get the console prompt back and there should
#           be any lockup and OSWR and DEVICE-OFF counters should increase
###############################################################################

# Begin L_DD_AUDIOPLAYBACK_0001

0000 set -x; is_android_ready.sh

# set a dummy wakelock to avoid going to suspend
0002 set -x; handlerAndroidPM.sh wakelock hold dummy

0004 set -x; handlerInputSubsystem.sh "keypad" "KeyCodePowerKey" 1 1 1 && sleep 2

# 0001-0003 are only for Demo purposes. They can be deleted
0001 set -x; wakeup_display.sh && sleep 1
0002 handlerAndroidMonkey.sh run 1 $UTILS_DIR_MONKEY/t2.ics.unlock.screen
0003 handlerAndroidMonkey.sh run 1 $UTILS_DIR_MONKEY/t2.ics.set.max.screen.timeout.value && sleep 1

# Set the max screen timeout (30 min)
0002 set -x; handlerAndroidSqlite3.sh set "system" "screen_off_timeout" "1800000"

# Verify that the screen is ON
0003 set -x; android_display_switch.sh ON

# Start Audio playback usecase: Play an MP3 for 15 seconds
0007 set -x; handlerActivityManager.sh audio_playback start $MP3_FILE

# The following events are triggered considering that the MP3 playback is ongoing

# Volume UP/DOWN 2 cycles
0008 set -x; android_volume_updown.sh 2

# Rewind the MP3 to start a new test
0009 set -x; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyPreviousSong && sleep 2

# Play/Pause 10 times
0009 set -x; android_playpause.sh 8 1

# Rewind the MP3 to start a new test
0009 set -x; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyPreviousSong && sleep 2
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 600 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 900 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 480 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 700 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 300 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 500 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 250 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 750 730
0009 set -x; handlerAndroidMonkey.sh touchscreen 1 500 600 730

# Stop playback and close Audio app
0008 set -x; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyPlayPause && sleep 1
0009 set -x; handlerAndroidMonkey.sh keypad 1 800 KeyMonkeyHome && sleep 2
0010 set -x; handlerActivityManager.sh audio_playback stop

# End L_DD_AUDIOPLAYBACK_0001
