#######################################
##Scenario: L_DD_GPIO_0015_0007
## Author: Andrii Danylov
## Date: 15 Aug 2012
## Testing: GPIO: IRQ support
## Verify that GPIO interrupts are only generated when the module using a particular GPIO pin is in use or activated
## 1) From all GPIO pins verify one in which the module using it can be disabled
## 2) Once the module using that GPIO pin is disabled verify the number of irq do not increment for a period of time
## 3) Enable the module using that GPIO pin and verify interrupts are incrementing
#######################################

# Begin L_DD_GPIO_0015_0007

0001 set -x; handlerIrqSupport.sh "get" "echo touchscreen" "1" "10" "371" && exit 0 || exit 1

# End L_DD_GPIO_0015_0007
