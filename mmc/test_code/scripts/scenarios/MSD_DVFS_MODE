###############################################################################
# Scenario: MSD_DVFS_MODE
# Author  : Andrii Danylov
# Date    : 02/04/13
# Testing : MMC: Power Management: DVFS
# Results : MMC driver shall allow DVFS during data transfer
#           DVFS stands for Dinamic Voltage Frequency Scaling.
###############################################################################

# Begin MSD_DVFS_MODE

0001 set -x; handlerCpuFreqScalFrequencies.sh run all "sleep 20" & timeout=0; iterations=2 ; while [  $timeout -lt $iterations ]; do handler_msd_rw.sh "1" "$MSD_TEST_FOLDER"; handler_msd_rw.sh "0" "$MSD_TEST_FOLDER"; timeout=$((timeout=$timeout+1)); done

0002 set -x; handler_msd_rw.sh "2" "$MSD_TEST_FOLDER"

# End MSD_DVFS_MODE
