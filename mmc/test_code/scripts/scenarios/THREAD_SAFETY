##################################################
# Scenario: THREAD_SAFETY
# Author:   Viswanath Puttagunta
# Date:     Mar 27, 2012
#################################################

# Begin THREAD_SAFETY

01 set -x; handlerCmd.sh "run" "dd if=/dev/urandom of=$MSD_TEST_FOLDER/$MMCSD_FILE_SIZE_BIG bs=1M count=20"
02 set -x; $MMCSD_DIR_HELPER/threadtest.sh $MMCSD_FILE_SIZE_BIG $MSD_TEST_FOLDER $MSD_TEST_FOLDER

# End THREAD_SAFETY
